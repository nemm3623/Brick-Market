spring:
  application:
    name: brick-market
  datasource:
    url: jdbc:h2:mem:brickmarket
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
