# 프로젝트 개요

- 프로젝트명: Brick-Market
- 중고 레고와 미개봉 레고 제품을 안전하게 거래할 수 있도록 중개하는 Spring Boot 기반 백엔드 프로젝트다.
- Java 21과 Spring Boot를 사용하며 도메인 중심 패키지 구조를 따른다.

# 작업별 필수 문서

- API, Controller, Domain, Service, Repository, Entity, JPA, Flyway 또는 데이터베이스 스키마를 변경할 때는 `docs/conventions.md`를 먼저 읽는다.
- API를 추가하거나 변경할 때는 `docs/api.md`의 구현 상태를 함께 갱신한다.

# 문서 해석

- 현재 사용자의 요청과 승인된 설계·계획을 작업 기준으로 사용한다.
- `docs/rationale/`은 규칙을 왜 그렇게 작성했는지 설명하는 학습용 문서다.
- `docs/superpowers/specs/`와 `docs/superpowers/plans/`는 승인된 설계와 구현 절차의 기록이다.
- 완료되거나 오래된 계획은 현재 구현 지침이 아니라 작업 기록으로 취급한다.
- 문서와 현재 코드, 테스트, Flyway 마이그레이션이 충돌하면 임의로 선택하지 말고 충돌 내용을 먼저 보고한다.

# Superpowers 작업 흐름

- 공개 API 계약, 데이터베이스 스키마, 인증·인가, 외부 의존성처럼 되돌리기 어려운 변경은 `superpowers:brainstorming`으로 요구사항과 범위를 정리한다.
- 여러 파일과 테스트 전략이 함께 바뀌는 큰 기능은 `superpowers:writing-plans`로 구현 계획을 작성한다.
- 구현은 위험도가 높거나 회귀 가능성이 큰 경우 `superpowers:test-driven-development` 흐름을 우선 검토한다.
- 버그나 테스트 실패처럼 원인 파악이 필요한 작업은 수정 전에 `superpowers:systematic-debugging`으로 원인을 확인한다.
- 완료를 보고하기 전에 `superpowers:verification-before-completion`으로 실제 검증 결과를 확인한다.
- 기존 계획이 현재 코드와 맞지 않으면 그대로 실행하지 않고 계획을 갱신하거나 새로운 계획을 작성한다.
- 문서 오타, 테스트 보강, 작은 리팩터링처럼 범위가 명확한 작업에는 필요한 최소 절차만 적용한다.

# 코드 작성 전

- 새 API, 클래스, 함수, 예외 코드를 만들기 전에 기존 코드를 검색한다.
- 유사한 구현이 있으면 프로젝트의 기존 패턴을 재사용하거나 확장한다.
- 재사용할 수 없다면 그 이유와 새로운 구조가 필요한 이유를 설명한다.
- 관련 테스트, `ErrorCode`, API 응답 구조, Entity와 Flyway 마이그레이션을 확인한다.
- 현재 작업과 관련 없는 리팩터링이나 파일 변경은 포함하지 않는다.

# 승인과 실행

- 새 기능, 공개 API 요청·응답, 데이터베이스 스키마, 외부 라이브러리, 인증·인가 정책을 변경할 때는 수정할 파일, 변경 내용, 예상 부작용과 검증 방법을 포함한 계획을 제시한다.
- 위 항목에 해당하는 변경은 사용자의 승인을 받기 전에는 실행하지 않는다.
- 테스트 보강, 문서 정리, 명백한 버그 수정처럼 범위가 작고 되돌리기 쉬운 변경은 작업 범위를 먼저 알리고 바로 진행할 수 있다.
- 승인된 계획 범위 안에서는 단계별 재승인 없이 구현과 검증을 진행한다.
- 한 번에 하나의 기능 단위 또는 하나의 문제 단위로 진행한다.
- 다음 항목이 달라지면 작업을 중단하고 변경된 계획을 다시 승인받는다.
  - 작업 범위
  - 공개 API 요청·응답
  - 데이터베이스 스키마
  - 외부 라이브러리 또는 서비스
  - 인증·인가 또는 보안 정책
  - 기존 기능의 호환성

# 검증 기준

- 새로운 동작과 버그 수정에는 관련 테스트를 추가한다.
- 변경 범위와 직접 관련된 테스트를 먼저 실행한다.
- PR 작성 전 또는 공유 전에는 전체 테스트를 실행한다.
- API 변경은 정상 요청과 주요 예외 응답을 검증한다.
- JPA, Flyway 또는 설정 변경은 전체 테스트와 기본 프로필 애플리케이션 기동을 확인한다.
- 실행하지 않은 테스트나 명령은 통과했다고 표현하지 않는다.

# 완료 보고

- 완료 보고에는 다음 내용을 포함한다.
  - 변경한 기능과 파일
  - 실제 실행한 검증과 결과
  - 남아 있는 제한사항 또는 위험
- PR 설명에는 긴 테스트 명령을 나열하지 않고 변경사항 및 검증 요약/결과만 간결하게 명사형 종결식으로 작성한다.
- 계획과 달라진 부분이 있다면 변경 이유를 함께 설명한다.

# 보안 및 금지 사항

- 실제 `.env`, 인증서, 토큰, 비밀번호 등 시크릿 파일의 내용을 읽거나 출력하지 않는다.
- 환경 변수 이름 확인을 위한 `.env.example` 같은 예시 파일은 사용할 수 있다.
- 사용자가 요청하지 않은 커밋, 푸시 또는 브랜치 삭제를 수행하지 않는다.
- `git reset --hard`, 강제 푸시 등 복구하기 어려운 명령을 실행하지 않는다.
- 기존 사용자 변경사항을 임의로 되돌리거나 덮어쓰지 않는다.
- `build/`, `.gradle/`, `.DS_Store` 같은 생성 파일을 커밋하지 않는다.
